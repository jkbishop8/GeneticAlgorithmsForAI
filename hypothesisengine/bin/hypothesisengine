#!/usr/bin/env ruby
require File.dirname(__FILE__) + '/../../lib/hypothesis-engine'
##Dir[File.join('../lib/hypothesis-engine/', 'lib', '*.rb')].each { |file| require file }

runner = HypothesisEngine::Runner.new(ARGV, STDIN, STDOUT)
runner.run
